version: '3'

services:
  node:
    image: node:12
    build:
      context: .
    command: sh -c "cd home/node/app && ./run.sh" 
    ports:
      - "3000:3000"
    volumes:
      - "./:/home/node/app"
      - "./node_modules:/home/node/app/node_modules"