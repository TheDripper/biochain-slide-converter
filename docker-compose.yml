version: '3'

services: 
  biochain:
    build:
      dockerfile: Dockerfile
      context: .
    image: bc_converter
    command: sh -c "npm run dev --loglevel verbose"
    ports:
      - 3000:3000
    volumes:
      - "./:/home"